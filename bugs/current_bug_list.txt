menu1
	regular
		YES/NO : ajouter "AUTOMATIC TIP" 


menu2
	il manque les cases "all" sur les tableaux
	Il faut supprimer la case "number of levers", qui existe déjà sous des noms différents pour chaque block. (number of pins, number of levers, number of discs
	regular
		row1
			100 : les valeurs 100 ne doivent pas apparaitre dans l'UI (mais doivent être renvoyée dans le script si la case est vide)
			number of levers : cette ligne existe déjà sous le nom "number of pins"
			pour les décimales, le point ne fonctionne pas, il faut mettre une virgule, et la valeur ne s'enregistre pas quand même
			distance from shoulder n'est pas calculé automatiquement dans le tableau
		row2 : la variable boléenne qt_regular_row2_cut_use_qt n'existe pas ? ni les suivantes
	lever lock
		angle of cutting : une seule case suffit qui renvoi sa valeur dans qt_lever_lock_row1_angle_on_y_qt
		dans le tableau, thickness doit s'afficher automatiquement, ainsi que Distance
	disklock pro (ne se sélectionne pas automatiquement quand on choisit disklock dans menu1)
		groove at the tip est un radio button qui donne le choix entre "YES" ou "NO" et renvoie dans qt_abloy_disklock_pro_groove_at_the_tip_qt 
	protec (ne se sélectionne pas automatiquement quand on choisit disklock dans menu1)
		RIGHT LEFT ne devrait pas être dans ce block
	axi home (ne se sélectionne pas automatiquement quand on choisit disklock dans menu1)
		supprimer la ligne et les variables associées à side of profile right left
		supprimer les lignes "number of levers"
	fichet 787
		cupprimer la ligne "number of levers"

		
		
		
		
